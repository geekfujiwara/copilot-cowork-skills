# Troubleshooting

## 顧客を抽出できない

期間と自社ドメイン設定を確認する。外部参加者がいない場合は空のレポートを作り、企業名を推測しない。

## 調査エージェントがない

`web_search` に切り替え、一次情報を優先する。同時実行は最大 3 件とし、レート制限時は逐次実行する。

## 公開情報が不足

期間、言語、キーワードを一つずつ緩める。それでも取得できなければ「直近の公開情報なし」と記載する。

## 送信できない

対話実行では宛先と本文の承認を取り直す。スケジュール実行では設定済み宛先以外へ変更しない。承認やポリシー制限を迂回しない。
